#include <corecrt_malloc.h>
#include <vector>

int main(int argc, const char** argv) {
	if (argc > 1 && argv[1][0] != 'h')
	while (true) {
		malloc(0x100000);
	}
	std::vector<char> mem;
	while (true)
	mem.push_back(0x00);
}